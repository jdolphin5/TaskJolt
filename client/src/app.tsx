import React, { useState, useEffect } from "react";
import MainContainer from "./components/MainContainer";

const App: React.FC = () => {
  return (
    <div style={{ padding: "0", height: "100%", backgroundColor: "gray" }}>
      <MainContainer />
    </div>
  );
};

export default App;
